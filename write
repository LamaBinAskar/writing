import 'package:flutter/material.dart';
import '../services/ble_audio_service.dart';

class HomeScreen extends StatefulWidget {
  final BleAudioService service;
  const HomeScreen({super.key, required this.service});

  @override
  State<HomeScreen> createState() => _HomeScreenState();
}

class _HomeScreenState extends State<HomeScreen> {
  String _status = 'جاهز للاستماع...';
  String _lastDetection = '';

  @override
  void initState() {
    super.initState();
    widget.service.addListener(_updateUI);
  }

  void _updateUI() {
    setState(() {
      _status = widget.service.isListening ? 'يستمع...' : 'جاهز';
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PulseHear'),
        backgroundColor: const Color(0xFF71A39E),
        foregroundColor: Colors.white,
      ),
      body: Padding(
        padding: const EdgeInsets.all(20),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Icon(
              Icons.hearing_outlined,
              size: 120,
              color: Color(0xFF005352),
            ),
            const SizedBox(height: 30),
            Text(
              _status,
              style: const TextStyle(
                  fontSize: 24, fontWeight: FontWeight.bold),
            ),
            const SizedBox(height: 20),
            Text(
              _lastDetection.isEmpty
                  ? 'انتظر إشارة من السوار'
                  : _lastDetection,
              style: const TextStyle(fontSize: 18),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 40),
            ElevatedButton(
              onPressed: () async {
                await widget.service.onDeviceSignal('adhan_detected');
                setState(() {
                  _lastDetection = 'تم تحليل الصوت بـ YAMNet!';
                });
              },
              style: ElevatedButton.styleFrom(
                backgroundColor: const Color(0xFF005352),
                foregroundColor: Colors.white,
                padding: const EdgeInsets.symmetric(
                    horizontal: 40, vertical: 15),
              ),
              child: const Text('اختبار YAMNet (أذان)'),
            ),
          ],
        ),
      ),
    );
  }

  @override
  void dispose() {
    widget.service.removeListener(_updateUI);
    super.dispose();
  }
}
